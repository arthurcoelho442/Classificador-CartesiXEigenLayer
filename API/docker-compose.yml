version: "3.9"

services:
  api:
    build:
      context: ./src/api
    container_name: api-container
    ports:
      - "5004:5004"
    volumes:
      - ./src/api:/api
    networks:
      - coprocessor-network
    restart: unless-stopped

networks:
  coprocessor-network:
    external: true
